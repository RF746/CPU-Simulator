#include <iostream>
#include <cmath>
#include <queue>
#include <cstdlib>
#include <iterator>

int main() {
    try {
        // Import numpy
        std::cout << "Successfully imported numpy." << std::endl;
    } catch(...) {
        std::cout << "Failed to import numpy." << std::endl;
    }

    try {
        // Import queue
        std::cout << "Successfully imported queue." << std::endl;
    } catch(...) {
        std::cout << "Failed to import queue." << std::endl;
    }

    try {
        // Import sys
        std::cout << "Successfully imported sys." << std::endl;
    } catch(...) {
        std::cout << "Failed to import sys." << std::endl;
    }

    try {
        // Import math
        std::cout << "Successfully imported math." << std::endl;
    } catch(...) {
        std::cout << "Failed to import math." << std::endl;
    }

    try {
        // Import random
        std::cout << "Successfully imported random." << std::endl;
    } catch(...) {
        std::cout << "Failed to import random." << std::endl;
    }

    try {
        // Import matplotlib
        std::cout << "Successfully imported matplotlib." << std::endl;
    } catch(...) {
        std::cout << "Failed to import matplotlib." << std::endl;
    }

    try {
        // Import itertools
        std::cout << "Successfully imported itertools." << std::endl;
    } catch(...) {
        std::cout << "Failed to import itertools." << std::endl;
    }

    return 0;
}
